# /setup-project コマンドメモ

事前に `docs/ideas/` にアイデアメモを作っておく必要がある。

`/setup-project` を実行すると、プロジェクトに必要な6つの永続ドキュメントを対話形式で自動作成する。

`docs/ideas/` の要件メモを読み込み、PRD（要求定義書）→ 機能設計書 → アーキテクチャ設計書 → リポジトリ構造 → 開発ガイドライン → 用語集の順に作成する。PRDのみユーザー確認が必要で、以降は自動生成される。完了後は `/add-feature` や `/review-docs` コマンドで開発を進められる。
